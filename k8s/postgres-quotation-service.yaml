apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-quotation-service
  namespace: insureflow
spec:
  serviceName: postgres-quotation-service
  replicas: 1
  selector:
    matchLabels:
      app: postgres-quotation-service
  template:
    metadata:
      labels:
        app: postgres-quotation-service
    spec:
      containers:
        - name: postgres-quotation-service
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: insureflow-secrets
                  key: postgres-db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: insureflow-secrets
                  key: postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: insureflow-secrets
                  key: postgres-password
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-quotation-service
  namespace: insureflow
spec:
  selector:
    app: postgres-quotation-service
  ports:
    - port: 5432
      targetPort: 5432